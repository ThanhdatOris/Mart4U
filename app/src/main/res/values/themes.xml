<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mart4U" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- HIện thanh trạng thái -->
        <item name="android:windowFullscreen">false</item>
        <item name="android:statusBarColor">#fa003e</item>

<!--        <item name="windowNoTitle">false</item>-->

<!--        <item name="android:actionBarStyle">@color/red</item>-->
        <!-- Tùy chỉnh thêm nếu cần -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="Theme.Mart4U" parent="Base.Theme.Mart4U" />
</resources>